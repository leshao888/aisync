# Profiles

简体中文 | [English](README.md)

本目录用于存放 built-in app profiles。

在 profile schema 和 validation rules 实现完成前，不要添加 production profiles。

Planned profiles：

- `codex.yaml`: v0.1 的 stable target
- `claude.experimental.yaml`: v0.2 计划支持
- `cursor.experimental.yaml`: 后续计划支持

